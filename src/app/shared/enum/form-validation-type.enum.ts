export enum FormValidationType {
  EMAIL = 'email',
  MAX = 'max',
  MAX_LENGTH = 'maxlength',
  MIN = 'min',
  MIN_LENGTH = 'minlength',
  PATTERN = 'pattern',
  REQUIRED = 'required',
  REQUIRED_TRUE = 'requiredTrue',
}
